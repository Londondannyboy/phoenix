[build]
builder = "railpack"

[deploy]
startCommand = "python3 -m streamlit run app.py --server.port ${PORT:-8501} --server.address 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
