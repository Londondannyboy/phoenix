{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python worker.py",
    "start": "python worker.py",
    "build": "echo 'No build step for Python'",
    "test": "pytest",
    "lint": "ruff check ."
  }
}
